services:
  dark-games:
    build: .
    container_name: dark-games
    restart: unless-stopped
    env_file: .env
    ports:
      - "3000:3000"
    environment:
      DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
